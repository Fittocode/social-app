<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>Document</title>
</head>
<body>
    <div class="container">
        {{#if user}}
        <h1>Welcome, {{user.username}}!</h1>
        <div>
            <a href="/{{user._id}}/add-post" class="btn btn-success">Add Post</a>
        </div>
        {{#each user.posts}}
        <div class="card mt-4">
            <div class="card-body">
                <h3 class="card-title">{{this.title}}</h3>
                <div class="card-text mb-2">
                <p>{{this.content}}</p>
                <p>{{this.likes}} likes, {{this.comments.length}} comments</p>
                </div>
                <a href="/update/{{this._id}}" class="btn btn-info">Update</a>
                <form action="/delete/{{this._id}}" method='post' class="d-inline">
                    <button type="submit" class="btn btn-danger">Delete</button>
                </form>
            </div>
        </div>
        {{/each}}
        {{else }}
        <p>Sorry, you must be logged in to view your profile!</p>
        {{/if}}
    </div>
</body>
</html>



