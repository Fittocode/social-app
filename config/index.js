const configureTemplating = require('./configureTemplating');
const configureParsers = require('./configureParsers');
const configurePassport = require('./passport');

module.exports = { configureTemplating, configureParsers, configurePassport }
